# خلاصه‌ی تغییرات — ۲۰۲۶-۰۸-۲۷

این فایل‌ها رو جایگزینِ فایل‌های هم‌نامشون تو ریپازیتوری کن (مسیرها دقیقاً
همون مسیرِ `src/...` هستن)، بعد commit/push. دو تا فایلِ `src/lib/autoProduce.js`
و `src/app/api/auto-produce/route.js` جدیدن، بقیه ویرایششون.

---

## ۱) بررسیِ Trend Finder

دو نکته‌ای که خودِ PROJECT_STATE.md به‌عنوان «حدسِ تأییدنشده» علامت زده
بود رو چک کردم — یکیشون واقعاً باگ بود، یکیشون نه:

- **`lib/trends/analyzer.js` → `generateText()`**: این تابع یک `system`
  می‌فرستاد که `router.js` اصلاً همچین پارامتری قبول نمی‌کرد — یعنی
  دستورالعملِ سختگیرانه‌ی «فقط JSON، بدون هیچ متنِ اضافه» هیچ‌وقت واقعاً
  به مدل نمی‌رسید (فقط پرامپتِ خودِ کاربر می‌رسید که خوشبختانه در انتها
  دوباره همین رو می‌خواد، برای همین فاجعه نبود، ولی قابلِ‌اعتمادتر نبود).
  حالا `generateText`/هر سه adapter (Groq، OpenAI، Anthropic) پارامترِ
  `system` رو واقعاً قبول و اعمال می‌کنن.
- **ایمپورتِ `getServerSession`/`authOptions`** تو `api/trends/scan-now/route.js`
  و `api/trends/[id]/route.js`: با الگویی که خودِ فایل `auth/authOptions.js`
  و بقیه‌ی ~۳۰ روتِ پروژه استفاده می‌کنن (`next-auth` بدونِ `/next`،
  `@/lib/auth/authOptions` بدونِ `.js`) یکی‌شون کردم.
- بقیه‌ی Trend Finder (منابع، امتیازدهی، ذخیره‌سازی، UI، اتصال به
  `/long` و `/short`) رو کامل خوندم — درست به‌نظر می‌رسه، باگِ دیگه‌ای
  پیدا نکردم.
- **یک باگِ اضافه که پیدا شد (نه تو خودِ Trend Finder، تو مقصدش)**:
  دکمه‌ی Approve کاربر رو به `/long?topic=...` یا `/short?topic=...`
  می‌فرسته، ولی `VideoStudio.js` هیچ‌وقت این query param رو نمی‌خوند —
  یعنی موضوعِ approve‌شده هیچ‌وقت خودکار تو فرم پر نمی‌شد. فیکس شد
  (پایین‌تر، بخشِ فایل‌های تغییریافته).

## ۲) خطای «پاسخ خالی از هوش مصنوعی دریافت شد» موقعِ نوشتنِ سناریوی شورت

**ریشه‌ی مشکل:** `openai/gpt-oss-120b` (مدلِ متنیِ Groq) یک reasoning
model واقعیه — همیشه مقداری توکنِ پنهان صرفِ «فکرکردن» می‌کنه قبل از
نوشتنِ جوابِ نهایی. طبقِ مستنداتِ Groq، وقتی `reasoning_effort` صراحتاً
ست نشه پیش‌فرض **medium**ه، نه low. یک تلاشِ قبلی (کامنتی که تو
`script/index.js` بود) فرض کرده بود `reasoning_effort="low"` واقعاً
فرستاده می‌شه و فقط بر همون فرض `maxTokens` رو از ۴۰۰ به ۷۰۰ برده بود
بالا — ولی خودِ پارامتر هیچ‌وقت واقعاً تو کدِ `registry.js` فرستاده
نمی‌شد. نتیجه: با سقفِ ۷۰۰ توکن، مدل با تلاشِ واقعیِ «medium» گاهی کاملِ
سهمیه رو صرفِ reasoning می‌کرد و `finish_reason="length"` می‌خورد
درحالی‌که هنوز حتی شروع به نوشتنِ جوابِ نهایی نکرده بود — یعنی `content`
کاملاً خالی برمی‌گشت.

اسکریپت‌های لانگ (سقفِ ۳۰۰۰ توکن) به همین دلیل تقریباً هیچ‌وقت این مشکل
رو نداشتن — حاشیه‌ی امنشون خیلی بیشتر بود.

**فیکس (سه لایه، تا هم ریشه درست بشه هم اگه بازم افتاد کلِ تولید نترکه):**
1. `registry.js`: حالا واقعاً `reasoning_effort: "low"` می‌فرسته.
2. `script/index.js`: سقفِ توکنِ شورت از ۷۰۰ به ۱۲۰۰ رفت بالا (حاشیه‌ی
   امنِ بیشتر، چون حتی با low هم مقداری نوسان هست).
3. `router.js`: یک باگِ جداگانه هم پیدا شد — یک پاسخِ *خالی* (نه یک
   exception) اصلاً «شکست» حساب نمی‌شد، پس حتی اگه یک provider دومی
   (مثلاً OpenAI یا Anthropic) هم تنظیم شده بود، هیچ‌وقت امتحان نمی‌شد.
   حالا پاسخِ خالی هم مثلِ بقیه‌ی خطاها رفتار می‌کنه: اول چندبار (۲ بار)
   خودِ همون provider دوباره امتحان می‌شه، بعد اگه بازم خالی موند می‌ره
   سراغِ provider بعدی.

## ۳) دکمه‌ی «تولید کاملاً خودکار»

یک بخشِ جدید (۰۰) بالای فرمِ `/long` و `/short` اضافه شد. ترتیب دقیقاً
همونیه که خواسته بودی:

**Trend Finder (بهترین موضوعِ approve‌شده) → سناریو → عنوان/تامبنیل/توضیحات/تگ
→ صدا → عکس یا کلیپ → رندر → زیرنویس (انگلیسی + ۵ زبان) → پستِ کامیونیتی
(فقط لانگ) → آپلود در یوتیوب.**

نکته‌های طراحی که خودم تصمیم گرفتم (چون گفتی خودم درستش کنم):

- **انتخابِ موضوع** فقط از بینِ موضوع‌های **approve‌شده**‌ی Trend Finder
  انجام می‌شه (بالاترین امتیاز اول) — یعنی گیتِ «تاییدِ دستیِ قبل از
  تولید» که خودِ پروژه از قبل عمداً داشت دست‌نخورده می‌مونه؛ این دکمه
  فقط قدم‌های *بعد از* approve رو خودکار می‌کنه. اگه هیچ موضوعِ
  approve‌شده‌ای برای اون حالت (لانگ/شورت) نباشه، یک پیامِ روشن می‌ده که
  اول باید از Trend Finder یکی رو approve کنی.
- انتخاب با یک UPDATE اتمیک تو دیتابیس انجام می‌شه (`FOR UPDATE SKIP LOCKED`)
  تا اگه دوبار پشتِ‌سرهم بزنی، دو بار رویِ یک موضوع کار نکنه.
- حریمِ خصوصی/زمان‌بندی/عکس‌یا‌کلیپ رو از همون تنظیماتی می‌خونه که تو
  بخشِ ۰۳ فرم انتخاب کردی (پیش‌فرضِ حریمِ خصوصی همون «خصوصی»یِ همیشگیِ
  فرمه) — یه سطحِ تنظیماتِ جداگانه نساختم چون بی‌دلیل بود.
- عمداً همیشه **in-process** اجرا می‌شه (نه از طریقِ worker/GitHub
  Actions)، حتی اگه `USE_RENDER_WORKER=true` باشه — دقیقاً همون انتخابی
  که «زمان‌بندِ خودکار» و «Repurpose» هم از قبل دارن. هم چون خودِ مسیرِ
  in-process (با heartbeat + self-ping) از قبل مشکلِ خوابیدنِ Render
  free tier رو حل کرده، هم چون قلاب‌کردنِ این ویژگی به تکمیل‌شدنِ
  async ورکر با باگِ شناخته‌شده‌ی «انقضای credential کال‌بکِ ورکر»
  (تو PROJECT_STATE.md، آیتمِ 🔴) تداخل می‌کرد.
- فرم زنده پر می‌شه: همون‌طور که موضوع/سناریو/عنوان انتخاب می‌شن، تو
  فیلدهای همون فرم ظاهر می‌شن — اگه چیزی دلخواهت نبود، بعد از تمومِ کار
  می‌تونی دستی از همون فرم اصلاح/دوباره امتحان کنی.

---

## فایل‌های تغییریافته/جدید

| فایل | نوع |
|---|---|
| `src/lib/providers/registry.js` | ویرایش — فیکسِ #۲ + پشتیبانیِ `system` |
| `src/lib/providers/router.js` | ویرایش — فیکسِ #۲ (retry/fallبackِ پاسخِ خالی) + پشتیبانیِ `system` |
| `src/lib/script/index.js` | ویرایش — سقفِ توکنِ شورت |
| `src/lib/trends/db.js` | ویرایش — سه تابعِ جدید برای claim/produce/release |
| `src/app/api/trends/scan-now/route.js` | ویرایش — فیکسِ ایمپورت |
| `src/app/api/trends/[id]/route.js` | ویرایش — فیکسِ ایمپورت |
| `src/app/long/page.js` | ویرایش — Suspense برای `useSearchParams` |
| `src/app/short/page.js` | ویرایش — Suspense برای `useSearchParams` |
| `src/components/studio/VideoStudio.js` | ویرایش — پرشدنِ خودکارِ موضوع + دکمه‌ی «تولید کاملاً خودکار» |
| `src/lib/autoProduce.js` | **جدید** — ارکستریشنِ موضوع→سناریو→متادیتا→pipeline |
| `src/app/api/auto-produce/route.js` | **جدید** — روتِ استریمِ NDJSON برای دکمه‌ی جدید |

همه‌ی فایل‌ها با `tsc --jsx` (برای JSX) و `node --check` (برای بقیه) از
نظرِ نحوی تست شدن؛ اجرای واقعی رو خودت باید تست کنی چون به دیتابیس/کلیدهای
API واقعی نیاز داره.
